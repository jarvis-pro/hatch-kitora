**/.git
**/.next
**/node_modules
# `.env` 已提交，仅含非 secret 的默认值 —— 允许进入构建上下文，
# 让 `next build` 能读到它。真实 secret（`.env.local` 及所有
# `.env.*.local` 变体）必须排除在外。
**/.env.local
**/.env.*.local
**/npm-debug.log
**/.DS_Store
**/coverage
**/dist
**/out
**/.vercel
**/.idea
**/.vscode
README.md
Dockerfile
docker-compose.yml
.github
