**/.git
**/.next
**/node_modules
# `.env` is committed and only carries non-secret defaults — let it into the
# build context so `next build` can read it. Keep secrets (`.env.local` and
# any `.env.*.local` variant) out.
**/.env.local
**/.env.*.local
**/npm-debug.log
**/.DS_Store
**/coverage
**/dist
**/out
**/.vercel
**/.idea
**/.vscode
README.md
Dockerfile
docker-compose.yml
.github
